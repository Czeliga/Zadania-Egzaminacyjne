1. SELECT imie, kolor, cena FROM klienci WHERE topKlient = "1";

2. SELECT imie, cena FROM klienci WHERE termin LIKE '2024-05-23%';

3. SELECT klienci.imie, klienci.kolor, ksztalty.ksztalt FROM klienci INNER JOIN ksztalty ON ksztalty_id = klienci.ksztalty_id WHERE ksztalty.ksztalt = "migdał";

4. ALTER TABLE wzory ADD doplaty INT; 